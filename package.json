{
  "name": "codebook-mock-server",
  "version": "1.0.0",
  "main": "index.cjs",
  "scripts": {
    "start": "npx json-server --watch db.json --port 8000 --middlewares ./node_modules/json-server-auth",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "json-server": "0.16.3",
    "json-server-auth": "2.0.1"
  }
}
