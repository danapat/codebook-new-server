{
  "name": "codebook-mock-server",
  "version": "1.0.0",
  "main": "index.cjs",
  "scripts": {
    "start": "json-server --watch db.json --port 8000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
